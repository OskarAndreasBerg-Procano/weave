---
description: mark a task or question complete and report what's left
argument-hint: <task or question id / short title>
---

Mark weave work complete: $ARGUMENTS

Resolve the referenced task/question with `weave set <id> --status done` (or `resolved`). Then run `weave status` and tell me what is left on the current focus plan.
