---
description: build the reading list (files Claude should load) for a task
argument-hint: <task id> [hint about which files matter]
---

Build the reading list for a task: $ARGUMENTS

Decide which files and folders are needed for that task, then `weave attach <taskId> <path>...`. Confirm with `weave context <taskId>` and show me the resulting reading list.
