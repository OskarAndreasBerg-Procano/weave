---
description: refresh tracked files, detect missing / renamed, optionally import new ones
---

Run `weave sync` to refresh tracked files and detect missing ones. If I ask for a full import, run `weave sync --scan`. Report what changed.
