---
description: free-text search across all graph node titles, bodies and tags
argument-hint: <query>
---

Search the weave graph (titles, bodies, tags) for: $ARGUMENTS

Run `weave search "$ARGUMENTS"` (use the `weave_search` MCP tool if available). Summarize the matches for me, grouped by node type, and propose what to do with each.
