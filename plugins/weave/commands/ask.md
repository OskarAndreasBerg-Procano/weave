---
description: park an open question in the graph so the human sees it in the workspace
argument-hint: <the question>
---

I have an open question for the human. Create it in the graph so it appears in their workspace: `weave add question "<question>" --author claude --body "<context / your recommendation>" --parent <focus plan id>`. Question: $ARGUMENTS
